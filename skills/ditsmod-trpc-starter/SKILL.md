---
name: ditsmod-trpc-starter
description: Installs a starter tRPC application.
---

## Installation

Using `@ditsmod/cli` (via `npx`):

```bash
npx @ditsmod/cli new my-app --template trpc-monorepo
```

